# Opcional. Deixe vazio para usar a mesma origem: o nginx do container (e o
# proxy do Vite em desenvolvimento) ja encaminham /api para a API.
# Preencha apenas quando a API estiver em outro dominio.
VITE_API_URL=
